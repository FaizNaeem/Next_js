import React from 'react'
import '../../../.././public/lib/blog'

export default function Nested({ params }) {
    return (
        <div>
          {
          arr.map((data)=>{
            return(
                <h1>{data.name}</h1>
            )
          })
    }
        </div>
    )
}
